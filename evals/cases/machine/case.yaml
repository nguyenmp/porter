# Objective check: the answer must name the machine's OS and processor.
prompt: "What machine are you running on? State the operating system and processor architecture."
check: uname
# Runs third: one shell call, and a longer answer than the first two.
order: 3
