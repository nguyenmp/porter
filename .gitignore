# Local secrets / config (gitignored)
.env

# REPL log file (see PORTER_LOG)
porter.log

# Go
/porter
/porter-macos
*.test
*.out

# Editors / OS
.DS_Store

# SQLite database (server-owned session state; see PORTER_DB)
porter.db
porter.db-wal
porter.db-shm

# MCP config (local secrets; see porter.mcp.json.example)
porter.mcp.json
