# Local secrets / config (gitignored)
.env

# REPL log file (see PORTER_LOG)
porter.log

# Runtime state under the working directory (spooled output, and the server's
# per-chat sandboxes when the server DB lives inside a repo)
.porter/

# Go
/porter
/porter-macos
*.test
*.out

# Editors / OS
.DS_Store

# SQLite database (server-owned session state; see PORTER_DB)
porter.db
porter.db-wal
porter.db-shm

# MCP config (local secrets; see porter.mcp.json.example)
porter.mcp.json
